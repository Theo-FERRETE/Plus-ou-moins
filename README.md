# Plus-ou-moins

<!-- Choisir un nombre aléatoire entre 1 et 100 (invisible pour l’utilisateur) : -->

<!-- Utiliser la fonction prompt() qui va déclencher une petite fenêtre où le user pourra dans un premier temps définir un chiffre minimum => quand il clique sur OK (stocker dans variable le min)=> une nouvelle fenêtre prompt apparait où il pourra choisir un max (stocke dans une variable le max)=> une fois l'intervalle défini, le programme se lance et choisit un chiffre aléatoirement (non visible par le user, stocker le chiffre dans une variable) => le jeu commence.

<!Script.js

Fichier contenant toute la logique de jeu avec les Fonctions Suivantes :

- Definir Min et Max
- dés que c'est Défini Générer aléatoirement un nombre dans la tranche définie.
- Input pour faire des propositions 
- Réponse Plus ou moins selon la proposition du Joueur
- Message de Victoire si la proposition est le nombre attendu
- Possibilité de rejouer avec un bouton 
- Limite de tentatives fixée à 10 par partie
>
-->